# 2026-07-17 1 号验收矩阵

## 1. 验收基线

- 验收分支：`dev`
- 提交：本地与远端 `dev@274d499` 已同步；当前文档分支从该提交创建
- GitHub：PR #1–#26 已合并；PR #25 同步代码，PR #26 固化目标分支规则，暂无可依赖的 CI 检查
- 主线：FastAPI + MySQL + Vue 3 + SHA-256 + 本地哈希链
- 公共验真地址：`http://10.160.211.35:5173/public/verify`；Vue 需使用 `--host 0.0.0.0`，`VITE_USE_MOCK=false`，手机与电脑处于同一 WLAN
- 测试数据：仅使用仓库中的模拟学生和模拟证书，不使用真实个人信息

## 2. 已完成的本地检查

| 检查项 | 结果 | 证据/说明 |
| --- | --- | --- |
| 后端自动化测试 | 通过 | 最新隔离验证为 `79 passed，5 skipped`；跳过项为缺少 `eth_tester` 的可选测试链测试 |
| 后端编译检查 | 通过 | `python -m compileall -q backend/app backend/scripts backend/tests` |
| 前端 TypeScript + Vite 构建 | 通过 | `1716 modules transformed`，构建成功 |
| Git 差异格式检查 | 通过 | `git diff --check` 无内容错误 |
| 前端构建风险 | 非阻塞 | 主 chunk 约 1.10 MB；Vite 提示可后续拆包，今天不阻塞课堂演示 |

## 3. 真实环境状态

| 检查项 | 状态 | 说明 |
| --- | --- | --- |
| MySQL 端口 3306 | 端口监听中（仅端口证据） | 只能证明端口有进程监听，不能证明项目数据库、账号和表结构可用 |
| `DATABASE_URL` | 阻塞 | 当前进程未设置；项目目录只有 `.env.example`，没有可用于验收的 `.env` |
| MySQL CLI | 阻塞 | 本机 PATH 中未发现 `mysql` 客户端 |
| FastAPI 8000 | 未启动 | 当前没有监听进程，未执行真实 API 回归 |
| Vue 5173 | 未启动 | 当前没有监听进程，未执行浏览器/手机回归 |
| 测试链 | 非阻塞 | `eth_tester` 测试跳过；本地哈希链仍是主线兜底 |

## 4. 端到端验收矩阵

| 编号 | 场景 | 负责人 | 当前状态 | 放行证据 |
| --- | --- | --- | --- | --- |
| E1 | 创建证书模板 | 1、2、4 | 待真实环境 | 管理员页面截图 + 数据库记录 |
| E2 | Excel 导入学生名单 | 2、4 | 待真实环境 | 导入结果 + `students` 表记录 |
| E3 | 创建批次并批量生成证书 | 2、4、5 | 待真实环境 | 批次记录 + PDF/二维码包 |
| E4 | 计算 PDF SHA-256 并写入本地回执 | 4、5 | 代码已具备，待真实环境 | 哈希清单 + `evidence_receipts` 记录 |
| E5 | 学生查询、下载和展示二维码 | 3、4 | 待服务启动 | 学生端截图 + 下载文件 |
| E6 | 编号/扫码验真 PASS | 3、4、5 | 已有单张历史 PASS 证据，今日未重跑 | 手机截图 + 接口响应 |
| E7 | 篡改 PDF 验真失败 | 3、4、5 | 自动化覆盖，待页面证据 | 原始/篡改 PDF + `HASH_MISMATCH` 页面 |
| E8 | 撤销后再次验真 | 3、4 | 待真实环境 | `REVOKED` 页面 + 撤销记录 + 审计日志 |
| E9 | 补发及新旧证书追踪 | 3、4、5 | 待真实环境 | 旧证书 `REISSUED`、新证书有效及关联字段 |
| E10 | ADMIN/TEACHER/AUDITOR 后端权限 | 2、4 | 待真实环境 | `401/403` 接口响应 + 前端角色截图 |
| E11 | 重启后 MySQL 持久化 | 1、4 | 阻塞 | 重启前后查询结果一致 |
| E12 | 证据索引、课程文档和答辩脚本 | 1，其他成员提供材料 | 进行中 | 文档目录、截图索引、演示脚本 |

## 5. PR #24 / #25 处理结论

- PR #24 标题为“补齐学生端与公共验真端验收状态覆盖”，作者为 `zhangsong-123`，目标分支为 `main`。
- PR #24 已误合并到 `main@3609e50`；随后通过 PR #25 同步并合并到 `dev@efe020d`。
- 同步过程中解决 `verification.ts`、`PublicVerifyView.vue` 冲突，清理重复 Merkle Proof 类型，修复 Mock 补发证书状态，并移除 `desktop.ini`。
- 当前 `origin/main` 已是 `origin/dev` 的祖先，`main` 没有任何 `dev` 缺失的提交；`dev` 反而包含管理员端 PR #23、PR #25 同步修复等更新。
- 同步后的验证结果为后端 `79 passed，5 skipped`、前端构建 `1716 modules transformed`、`compileall` 和差异检查通过。

## 6. 今日放行结论

当前结论：**代码和协作规则已统一到 `dev@274d499`，但真实环境业务验收仍未完成；不能据此宣称完整闭环已验收。**

继续验收前必须由 4 号提供可用的数据库配置和启动方式，由 1 号确认不包含真实密码或密钥；随后启动 FastAPI/Vue，完成 E1–E11 的真实证据收集。
