# 5号证书生成与存证今日交付 - 2026-07-20

按今日分工文档"五、今晚统一收成果格式"的五点回收。

## 一、已完成内容

本地环境（`dev` 分支，已同步 PR #27/#28/#29）下，完整跑通一轮证书生成收尾链路：

1. **证书生成稳定产出 PDF + 二维码**
   - 新建批次 `batch_id=5`（模拟学生 张三/2023101、李四/2023102），调用生成接口，`generated_count` 与学生数一致。
   - 产出 `CERT-20260720-0001`（张三）、`CERT-20260720-0002`（李四），PDF 与二维码文件均正常生成在 `outputs/certificates/`。

2. **`certificate_hash` / `receipt_id` / `status` 与证书记录核对一致（对应今日任务 E4）**
   - `CERT-20260720-0001`：`certificate_hash = 53464a6f8f2e528ebd9c4bd90174cf278a71ba671a015db3f7c61171a6d6c4dc`，本地对 PDF 重新计算 `shasum -a 256` 结果完全一致。
   - `CERT-20260720-0002`：`certificate_hash = c9b270e726e1925f47fccb070611ae993561c2d4c408687cd3c9d2450d91cfb6`，同样核对一致。
   - 两张证书 `receipt_id`（`RCP-20260720-0005`/`0006`）、`status=VALID`、`evidence_status=CONFIRMED` 均正常。

3. **异常样例：篡改 PDF 验真失败（对应今日任务 E7）**
   - 对 `CERT-20260720-0001.pdf` 追加字节生成 `CERT-20260720-0001_tampered.pdf`，调用 `POST /api/verification/{certificate_no}/file` 上传验真。
   - 返回 `result: HASH_MISMATCH`，`hash_match: false`，`message: "上传文件与存证版本不一致，可能已被篡改。"`，篡改检测符合预期。

4. **撤销 + 补发追踪（对应今日任务 E9）**
   - 撤销 `CERT-20260720-0001` → 状态变为 `REVOKED`。
   - 补发后：旧证书状态变为 `REISSUED`，`new_certificate_no = CERT-20260720-0003`；新证书 `CERT-20260720-0003` 状态 `VALID`，`previous_certificate_no = CERT-20260720-0001`，新旧证书双向关联字段正确。

## 二、还没完成但不影响主流程的内容

- **Merkle Root / 本地测试链上链**：本批次（`batch_id=5`）未调用 `POST /admin/batches/{batch_id}/merkle-root`，`root_id` 为空。这是独立可选步骤，此前（7-15）已验证过链路可用，今天优先保证 PDF/哈希/回执/异常场景主线，未重复跑。
- **线上服务器环境（`verify.lotusrain.net`）联调**：今天团队一度计划切到线上服务器测试，但服务器端出现问题，改回本地环境完成上述验证；如果服务器修复，需要用同样的步骤在线上环境重新跑一遍产出正式版证据。
- **手机扫码实测**：本轮为本机 API 级别验证，未重新做手机扫码（此前 7-16 已用手机验证过公共验真页可用，逻辑未变，未重复）。

## 三、当前阻塞点

- 无实质阻塞。本地 MySQL/FastAPI 环境正常，权限（`demo-admin-token`）验证正常。
- 小插曲：Swagger UI 手动填写 `authorization` header 时未生效（页面显示已填但请求未携带），后续全部改用终端 `curl` 直接带 header，问题绕过，不影响结果。

## 四、需要别人配合的地方

- **重要说明**：以上全部验证都是在我自己本机（本地 MySQL + 本地 FastAPI，`127.0.0.1`）独立跑的，数据只存在我本地数据库里，其他人本机/共享环境里查不到这几张证书，不能直接拿证书编号去核对。
- **3号**：暂时没法请你核对，等共享环境（线上服务器或共用数据库）就绪后，我会用同样的步骤重新生成一批，到时候数据是大家都能看到的，再麻烦你在公共验真页核对哈希和状态展示是否一致。
- **1号/4号**：线上服务器或共用数据库好了之后通知我，我按今天同样的步骤在共享环境里重新跑一遍，出一版真正能交叉验证的正式证据。

## 五、可直接发群里的材料

- 证书样例：`outputs/certificates/CERT-20260720-0001.pdf`、`CERT-20260720-0002.pdf`、`CERT-20260720-0003.pdf` 及对应二维码。
- 篡改样例：`outputs/certificates/CERT-20260720-0001_tampered.pdf`（上传后返回 `HASH_MISMATCH`）。
- 存证说明（一句话）：证书生成时对 PDF 计算 SHA-256 写入 `certificate_hash` 并生成本地哈希链回执；验真时重新计算上传文件哈希与存证哈希比对，一致返回 `PASS`，不一致返回 `HASH_MISMATCH`；撤销/补发通过 `previous_certificate_no`/`new_certificate_no` 双向关联新旧证书。

## 安全说明

- 全程使用模拟学生数据（张三/2023101、李四/2023102），未使用任何真实学生信息。
- 未提交 `.env`、真实密钥、Token。
