# 2026-07-23 今日开发任务分工

## 一、今日目标

从“管理员可登录、线上服务健康”推进到“团队账号可协作、固定数据可演示、异常状态可复验”。今天不新增业务大功能，重点是用服务器上的模拟数据完成一轮可重复的线上验收。

## 二、统一基线与边界

- 代码基线：`origin/dev@e7c7ebe`；线上 release：`20260722-162118-e7c7ebe`。
- 管理登录：`https://verify.lotusrain.net/login`；公共验真：`https://verify.lotusrain.net/public/verify`。
- 管理员已登录线上后台。团队成员只能使用管理员创建的一次性教师邀请链接，禁止共享管理员密码。
- 使用 5-10 名模拟学生、模拟机构和模拟证书。不得上传真实学生材料、密钥、token 或把邀请码写入文档/截图。
- `main` 暂不更新；所有提交继续进入 `dev`，只有完成本文件的验收门槛后才准备 `dev -> main`。

## 三、五人任务分工

| 负责人 | 今日任务 | 必须交付 | 验收标准 |
| --- | --- | --- | --- |
| 1 号 | 创建并回收教师邀请；冻结今日演示项目、模板、模拟名单和证书编号；组织一次线上全流程彩排；更新进度与风险 | 演示数据索引、联调结论、阻塞清单 | 至少一名教师账号完成注册并参与一次完整操作；所有证据指向同一服务器数据 |
| 2 号 | 用受邀教师账号回归管理端：登录、项目、学生、模板、批次、签发和回执；记录教师点击“操作日志”菜单被路由拒绝的前端一致性问题；检查管理员禁用教师后的前端退出 | 教师端截图、管理员/教师菜单与权限表、前端问题清单 | 页面刷新后数据仍在；无令牌 `401`；教师不能进入账号管理；不出现笼统 `Network Error` |
| 3 号 | 验收公共验真和 PDF 上传复验；完成二维码扫码、编号查询、原 PDF、篡改 PDF、撤销和补发新旧证书展示 | 公共验真截图/录屏、异常结果表、扫码证据 | 覆盖 `PASS`、`HASH_MISMATCH`、`REVOKED`、`REISSUED`、`NOT_FOUND`；上传篡改不得改变证书主记录、哈希、回执和状态，新增验真审计日志属于预期 |
| 4 号 | 回归线上 MySQL、认证和 RBAC：邀请一次性、账户禁用令牌失效、教师权限；若已有受控 `AUDITOR` 账号，再验证其只读边界；检查审计日志和迁移健康 | 接口状态码记录、账号/RBAC 回归表、后端测试或接口日志摘要 | 管理 API 无 token `401`；邀请不可重复使用；禁用后旧 token 失效；已有审计员越权为 `403`，没有审计员则如实记录为待补账号创建能力 |
| 5 号 | 在服务器生成固定证书包；核对 PDF、二维码、SHA-256、回执和本地哈希链；准备一张撤销和一组补发样例 | 证书编号清单、PDF/二维码/哈希/回执索引、篡改样本和本地链说明 | 每张演示证书可追踪到状态和回执；二维码指向 HTTPS 公共验真；测试链不可用时本地哈希链完整可演示 |

## 四、执行顺序

1. **账号门禁：1、2、4 号**。管理员创建教师邀请，2 号完成注册和登录；4 号记录邀请、教师权限与会话失效结果。审计员仅在已有受控账号时参与回归，不得手工改库造号。
2. **数据门禁：1、5 号**。创建项目、启用模板、导入模拟 Excel，固定批次和证书编号。
3. **业务彩排：2、5 号**。批量签发并确认 PDF、二维码、哈希和回执；随后执行批量/单张存证。
4. **公共验真：3、5 号**。确认原 PDF `PASS`、篡改 PDF `HASH_MISMATCH`、撤销 `REVOKED`、补发旧证书 `REISSUED` 和新证书 `PASS`。
5. **材料收口：全员，1 号汇总**。提交截图、接口摘要和编号索引；不提交真实文件、邀请 token、密码或数据库导出。

## 五、今日禁止事项

- 不开放学生自助注册，不使用 `student_no` 作为正式身份认证。经项目负责人批准，临时公网学生演示仅可使用模拟数据，范围固定为 `/student/*` 以及 `/api/student/certificates` 和其子路径。
- 不接入真实主网、不申请或传播钱包私钥、不因测试链不稳定中断本地哈希链演示。
- 未经项目负责人批准不得扩大 Caddy 白名单；本次仅增加临时学生演示路径，不暴露 `/docs`、`/openapi.json`、健康接口或数据库端口。
- 不提前把 `dev` 合并到 `main`，不因页面可访问而跳过角色、异常和真实数据回归。

## 六、收工验收门槛

今天结束前，至少要有一套同源证据证明：受邀教师注册成功 -> 导入模拟名单 -> 生成证书 PDF/二维码/哈希/回执 -> 公共验真通过 -> 篡改失败 -> 撤销和补发状态正确；同时记录未登录 `401`、邀请码不可重复使用。若已有受控审计员，再记录其越权 `403`；否则将审计员创建能力列为缺口。未达到时，明确记录缺口，不将项目写成“最终验收完成”。
