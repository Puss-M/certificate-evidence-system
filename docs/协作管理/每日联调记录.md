# 每日联调记录

本文档由 1 号负责人维护，用于记录每天集成进展、接口问题、风险和次日任务。每天至少更新一次。

## 记录模板

```text
## YYYY-MM-DD 联调记录

### 今日目标
- 

### 今日集成结果
| 模块 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- |
| 管理端前端 | 2 号 | 未开始/进行中/已集成/有阻塞 |  |
| 学生与验真端 | 3 号 | 未开始/进行中/已集成/有阻塞 |  |
| 后端业务接口 | 4 号 | 未开始/进行中/已集成/有阻塞 |  |
| 证书生成与存证 | 5 号 | 未开始/进行中/已集成/有阻塞 |  |

### 已确认接口或字段
- 

### 发现的问题
| 问题 | 影响 | 负责人 | 处理计划 |
| --- | --- | --- | --- |
|  |  |  |  |

### 今日可演示流程
- 

### 明日重点
- 
```

---

## 2026-07-13 联调记录

### 今日目标

- 建立 GitHub 主仓库和协作规则。
- 明确 5 人分工和 1 号负责人的总控职责。
- 先冻结最小闭环：Excel 导入、证书生成、PDF/二维码、SHA-256、本地哈希链、扫码验真、撤销。

### 今日集成结果

| 模块 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- |
| 总体设计与 Git | 1 号 | 已开始 | 已创建 GitHub 仓库并推送初始文档 |
| 管理端前端 | 2 号 | 待开始 | 等待接口规范和页面范围确认 |
| 学生与验真端 | 3 号 | 待开始 | 等待验真接口和演示数据 |
| 后端业务接口 | 4 号 | 待开始 | 等待数据库设计和接口规范 |
| 证书生成与存证 | 5 号 | 待开始 | 等待证书编号、PDF 路径和存证接口口径 |

### 已确认接口或字段

- 核心字段：`certificate_no`、`student_id`、`batch_id`、`pdf_path`、`certificate_hash`、`receipt_id`、`status`。
- 证书哈希口径：优先对最终 PDF 文件计算 SHA-256。
- 存证策略：先实现本地哈希链，FISCO BCOS 作为加分项。

### 发现的问题

| 问题 | 影响 | 负责人 | 处理计划 |
| --- | --- | --- | --- |
| FISCO BCOS 工作量偏大 | 可能拖慢最小闭环 | 1 号、5 号 | 先做本地哈希链兜底，FISCO 后置 |
| 前后端接口尚未冻结 | 容易联调失败 | 1 号、4 号 | 先维护接口规范文档 |
| 数据库字段尚未最终确认 | 影响后端和证书模块 | 1 号、4 号、5 号 | 以数据库设计文档为初版基准 |

### 今日可演示流程

- 当前还没有业务系统可演示，已完成项目规划和协作准备。

### 明日重点

- 后端创建基础项目和数据库连接。
- 前端确认管理员端、学生端、验真端页面清单。
- 证书模块确认 PDF 样式、编号规则和二维码链接格式。
- 1 号负责人收集各模块接口需求并冻结第一版接口。

## 每日必填检查项

从进入联调阶段开始，每天必须补充以下四项，避免记录流于形式：

| 检查项 | 填写要求 |
| --- | --- |
| 今天跑通的主流程 | 例如“Excel 导入 -> 生成证书记录”或“扫码验真显示有效” |
| 今天阻塞的接口 | 写明接口路径、字段、负责人和影响范围 |
| 今天确认的验收点 | 写明明天如何判断该问题已经解决 |
| FISCO 是否影响主线 | 只能填“不影响，本地哈希链可演示”或“影响，需立即降级处理” |

### 每日主流程进度表

| 日期 | 已跑通主流程 | 阻塞接口/模块 | 负责人 | 明日验收点 |
| --- | --- | --- | --- | --- |
| 2026-07-13 | 项目仓库和协作文档初始化 | 暂无业务接口 | 1 号 | 后端和证书模块确认第一版字段 |

## 2026-07-14 联调记录

### 今日目标

- 按冻结字段和接口规范完成第一轮工程落地。
- 建立 FastAPI 后端骨架、Vue 3 管理端页面、证书生成与本地哈希链原型。
- 打通学生数据、证书编号、PDF/二维码、SHA-256、回执和验真接口的第一版契约。

### 今日集成结果

| 模块 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- |
| 总体设计、系统集成与 Git | 1 号 | 进行中 | 冻结技术栈、字段和接口规范，推进当日 PR #3—#10 合并与冲突处理；#11 属于次日合并 |
| 管理端前端 | 2 号 | 基础版已交付，契约已对接，待真实数据回归 | 完成 9 张管理端页面截图、路由清单、Mock 数据和接口需求清单；当晚已完成管理端契约对接，但仍有演示兼容数据 |
| 学生与验真端 | 3 号 | 未见独立前端交付证据 | 后端公共验真接口已存在，学生端前端页面尚未见当日独立交付 |
| 后端业务接口 | 4 号 | 已集成基础版 | FastAPI 骨架、MySQL 配置、统一响应、CORS、异常处理、核心模型和最小接口完成；交付记录为 `15 passed` |
| 证书生成与存证 | 5 号 | 已集成原型 | 证书生成、本地哈希链和 FastAPI 服务接入已形成原型；批量生成、并发重试和接口联调继续推进 |

### 已确认接口或字段

- 冻结 `certificate_no`、`student_id`、`student_no`、`student_name`、`batch_id`、`template_id`、`pdf_path`、`certificate_hash`、`qr_code_path`、`verify_url`、`receipt_id`、`status`。
- 统一响应外层为 `{code, message, data}`，分页数据使用 `records/total/current/size`。
- 后端基础接口包括健康检查、学生列表、证书列表、编号验真和上传 PDF 验真。
- 前端初版仍使用 Mock，切换真实 FastAPI 需要设置 `VITE_USE_MOCK=false`。

### 发现的问题

| 问题 | 影响 | 负责人 | 处理计划 |
| --- | --- | --- | --- |
| 管理端仍有 Mock/演示兼容数据 | 刷新后部分数据不持久化，不能代表最终真实联调 | 2、4 号 | 后端接口稳定后切换真实模式并重新回归 |
| 批次路径和接口契约仍在收敛 | 次日继续修正批次接口和字段契约，可能影响前端请求 | 1、2、4 号 | 次日继续核对批次和二维码契约 |
| 学生端前端尚无交付证据；公共验真、撤销/补发已有后端接口或契约但无端到端页面验收 | 主线仍不能完整演示 | 3、4、5 号 | 次日优先补学生/验真页面和异常状态验收 |
| 测试链、Merkle、FISCO 尚未进入主线 | 可能拖慢最小闭环 | 1、5 号 | 本地哈希链优先，链上功能后置 |

### 今日可演示流程

- 管理端页面原型和 Mock 数据展示。
- FastAPI `/api/health`、学生列表、证书列表和基础验真接口。
- 单张证书 PDF、二维码、SHA-256 和本地回执原型。

### 明日重点

1. 将管理员端与 FastAPI 真实接口接通。
2. 完成 Excel 导入、批次创建和批量生成接口。
3. 完成证书下载、公共验真和上传 PDF 哈希复验。
4. 保持本地哈希链兜底，不让测试链阻塞主线。

## 2026-07-15 联调记录

### 今日目标

- 在 7 月 14 日骨架基础上完成学生端、公共验真端和管理员端真实接口联调。
- 修复学院字段、学生删除、批次生成、撤销补发和角色跳转问题。
- 完成证书样例、哈希回执、测试链增强和交付证据整理。

### 今日集成结果

| 模块 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- |
| 总体设计、系统集成与 Git | 1 号 | 已完成当日合并收口，待最终演示回归 | 合并并核对 PR #16—#22，统一接口契约和风险口径；这些 PR 无正式 Review/CI checks 证据；最终 MySQL/浏览器/手机回归仍待执行 |
| 管理端前端 | 2 号 | 已集成，待最终回归 | PR #18/#21/#22：交付文档标注为 7 张真实接口截图，另完成交付文档、批量删除、角色跳转和 403 返回修复；构建通过，但截图不能独立证明最终 MySQL 持久化 |
| 学生与验真端 | 3 号 | 已集成，待设备回归 | PR #16：学生中心、证书详情、下载、二维码、公共编号验真和上传 PDF 验真；身份仍使用演示版 `student_no` |
| 后端业务接口 | 4 号 | 已集成，待最终数据库回归 | PR #17/#19：学院字段持久化、迁移保护、删除/补发/批次生成异常修复、统一冲突响应和测试 |
| 证书生成与存证 | 5 号 | 已集成，待最终演示回归 | 交付文档/PR 正文自报生成 8 张证书样例并核对 PDF SHA-256、本地哈希链回执和篡改文件失败；PR #20 增加批次 Root 可选测试链上链和降级策略 |

### 已确认接口或字段

- 学生端接口：`/api/student/certificates` 列表、详情、下载和二维码，并通过 `student_no` 做演示归属校验。
- 公共验真：`GET /api/verification/{certificate_no}` 和 `POST /api/verification/{certificate_no}/file`，返回 `PASS`、`HASH_MISMATCH`、`REVOKED`、`NOT_FOUND`、`NO_RECEIPT` 等状态。
- 学院字段 `college` 已从 Excel、管理端表单、数据库模型和迁移脚本贯通。
- 撤销后补发保留 `previous_certificate_no`，旧证书置为 `REISSUED`；新证书 PDF、哈希、二维码和回执的完整产物链仍需最终数据库/浏览器回归确认。
- PR #20 的测试链只写批次 Merkle Root，未配置或失败时自动降级到本地 Root/哈希链。

### 发现的问题

| 问题 | 影响 | 负责人 | 处理计划 |
| --- | --- | --- | --- |
| 删除和补发曾出现 `Network Error` | 管理端无法确认操作结果 | 2、4 号 | 已修复代码并补充测试；仍需最终 MySQL、FastAPI 和浏览器回归 |
| 学院字段旧库缺列 | Excel 导入和编辑后刷新丢失学院 | 4 号 | 已加入安全补列迁移并验证幂等 |
| 学生端没有正式登录 | `student_no` 不能作为生产鉴权 | 3、4 号 | 仅保留演示边界，不阻塞当前 P0；后续接 token 身份 |
| 旧二维码可能仍指向旧验真地址 | 手机扫码可能进入旧 API 路径 | 3、5 号 | 配置最终 `PUBLIC_VERIFY_BASE_URL` 后重新生成固定演示二维码 |
| 测试链是内存运行 | 重启后链上数据会丢失 | 5 号、1 号 | 答辩使用本地哈希链兜底，不把测试链写成生产主网 |

### 今日可演示流程

- 管理员页面：交付文档标注为登录、首页、学生、批次、证书、存证回执、操作日志 7 张真实接口截图；截图本身不能独立证明 MySQL 持久化。
- 学生端代码流程：交付记录描述了查询证书、下载 PDF、展示二维码和分享验真链接。
- 公共验真代码测试：交付记录描述原 PDF 通过、篡改 PDF 失败、撤销证书显示已撤销、未知编号提示不存在；仍需浏览器/设备回归。
- 证书生成与存证：5 号交付文档/PR 正文自报 8 张模拟证书、SHA-256、本地回执连续性和可选 Root 上链样例，样例不属于仓库独立验收证据。

### 明日重点

1. 以最新 `dev` 启动最终 MySQL、FastAPI 和 Vue，避免用 Mock/DEMO 数据做验收。
2. 完成删除、撤销、补发、新旧证书公共验真和审计日志的固定数据回归。
3. 完成手机扫码、旧二维码重生成和学生/公共验真截图。
4. 补齐详细设计、测试计划、部署说明和答辩材料。

## 2026-07-16 联调记录

### 今日目标

- 按 1—5 号职责对当前版本做一次收口审计，不再扩展新的主线功能。
- 用自动化测试和现有演示产物核对证书、哈希、回执、验真、撤销补发和前端页面的完成情况。
- 明确最终 MySQL、浏览器和手机扫码验收的剩余阻塞，并分配负责人。

### 今日集成结果

| 模块 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- |
| 总体设计、系统集成与部署 | 1 号 | 本地收口完成，待最终环境验收 | 已完成本轮状态审计、依赖恢复、自动化验证和收口记录；最终演示环境、课程文档和部署材料仍需补齐 |
| 管理端前端 | 2 号 | 已集成，待最终回归 | 页面、角色路由、批量删除、撤销和补发代码已合入；恢复依赖后生产构建已通过 |
| 学生与验真端 | 3 号 | 已集成，单张手机实扫通过，待完整异常回归 | 学生中心、下载、二维码和公共验真页面已存在；`CERT-20260716-0001` 手机扫码进入验真页并显示通过 |
| 后端业务接口 | 4 号 | 已集成，待最终数据库/浏览器回归 | 当前后端测试 `77 passed，5 skipped`；跳过项为缺少 `eth_tester` 的测试链增强测试 |
| 证书生成与存证 | 5 号 | 已集成，单张二维码已实扫通过，待最终回归 | 证书 PDF、二维码、SHA-256、本地回执及批次 Root 代码和样例已存在；仍需核对旧二维码、补发证书和异常状态 |

### 已确认接口或字段

- 主链路字段仍统一使用 `certificate_no`、`student_id`、`batch_id`、`pdf_path`、`certificate_hash`、`receipt_id`、`status`。
- 编号验真和上传 PDF 复验继续区分：前者校验服务端留存文件，后者校验用户上传文件。
- 撤销后状态为 `REVOKED`；补发保留旧证书并将旧证书状态置为 `REISSUED`，新证书使用新的编号、PDF、哈希和回执。
- 学生端当前使用 `student_no` 作为模拟身份参数，不视为正式登录或生产级身份隔离。

### 发现的问题

| 问题 | 影响 | 负责人 | 处理计划 |
| --- | --- | --- | --- |
| 最终 MySQL + FastAPI + Vue 固定数据全流程尚未形成一套完整证据 | 影响最终答辩可重复演示 | 1、4 号 | 启动最终环境后跑通导入、签发、存证、下载、验真、撤销、补发并留存截图 |
| 已有一张新证书二维码完成手机实扫，但旧二维码和异常状态尚未覆盖 | 影响完整现场扫码演示 | 3、5 号 | 保留 `CERT-20260716-0001` 实扫证据；继续核对旧二维码、撤销/补发二维码和最终局域网地址 |
| 前端主 JavaScript chunk 约 1.10 MB | 非阻塞性能风险，不影响课堂演示 | 2 号 | 后续按需使用动态导入和依赖拆包，当前不阻塞主线 |
| 详细设计、测试计划、答辩 PPT 和完整演示证据包尚未齐全 | 影响课程最终提交 | 1 号统筹，各模块提供事实和截图 | 先补详细设计和测试计划，再整理 PPT 与固定演示脚本 |

### 今日可演示流程

- 后端自动化测试：`77 passed，5 skipped`，后端编译检查通过。
- 前端恢复依赖后生产构建通过：`1715 modules transformed`，构建产物生成成功。
- 已有管理员端真实接口页面截图、学生/公共验真端代码与测试，以及证书 PDF/二维码/哈希/本地回执样例；学生/公共端专用截图仍需补齐。
- 用户提供的人工手机实扫证据：`CERT-20260716-0001` 成功进入公共验真页，显示“证书有效、哈希一致、回执存在”。这证明单一样例的二维码访问和公共验真 PASS 链路可用。
- 最终 MySQL 浏览器回归、其他证书/旧二维码/补发二维码，以及异常状态仍不能写成已验收完成。

### 明日重点

1. 使用固定模拟数据在最终 MySQL 环境跑完整闭环，补齐撤销、补发和异常分支证据。
2. 扩展手机扫码验证到旧二维码、撤销/补发和异常状态，并确认最终地址。
3. 更新每日联调记录、详细设计、测试计划和答辩材料。

### 1 号收口结论

- 当前检出分支为 `dev`，HEAD 与 `origin/dev` 同步在 `ab258dc`；`main` 是 `dev` 的祖先，后续验收以 `dev` 为准。
- `dev` 已合入学生端/公共验真、批量证书与本地存证、Merkle/测试链适配、管理员端删除/补发/角色权限修复及 7 月 15 日交付文档。
- 按 `dev` 代码和自动化测试重新评估：P0 核心实现（代码口径）约 95%，联调验收约 65%—70%，课程最终材料约 50%，综合完成度约 78%；这些比例不等同于最终现场验收通过率。
- 当前主线没有发现需要重构或新增大功能的问题；剩余工作集中在最终 MySQL/浏览器/手机回归、学生与公共验真截图、课程文档、答辩材料和部署收口。

## 2026-07-16—2026-07-17 两日收口分工（以 dev 实际 PR 和代码为准）

### 分工依据

- 当前验收基线为 `dev@274d499`；本地 `dev` 已完成归档并快进，文档交付分支从该提交创建。
- GitHub PR #1—#26 已合并；PR #25 同步 PR #24 内容，PR #26 固化所有 PR 先进入 `dev` 的规则。
- 最近 PR 实际落点：#16 学生端/公共验真（3 号），#17/#19 后端与前后端联调（4 号主责、2 号配合），#18/#21/#22 管理员端截图、交付和权限修复（2 号），#20 测试链/批次 Root（5 号）。
- P2 的 Merkle/测试链已经存在，未来两天不再新增链上功能，除非只做稳定性验证。

### 7 月 16 日：环境与功能回归日

| 负责人 | 今日任务 | 今日必须交付 | 验收标准 |
| --- | --- | --- | --- |
| 1 号 | 固定 `dev` 版本；建立 P0 验收矩阵；统一后端、前端、数据库和二维码地址；检查空库 DEMO 回退风险；更新联调记录 | 一份可执行验收清单、固定演示数据、阻塞责任表 | 所有人使用同一版本和字段；每个未完成项有负责人；不把 DEMO 回退当成真实联调 |
| 2 号 | 以真实接口模式回归管理员端；确认最终环境 `VITE_USE_MOCK=false`（`.env.example` 默认值需特别核对）；验证导入、批次、证书、回执、日志、删除、撤销、补发和角色权限；保留构建结果 | 管理端回归截图；生产构建日志；前端问题清单 | 页面刷新数据仍在；错误提示清楚；`npm run build` 通过；截图来自真实接口 |
| 3 号 | 回归学生列表、详情、下载、二维码和公共验真；验证 PASS、HASH_MISMATCH、REVOKED、REISSUED、NOT_FOUND、NO_RECEIPT | 学生端/公共验真截图；异常用例表；原始和篡改 PDF | 原文件通过、篡改失败、撤销后不显示有效；接口和页面字段一致 |
| 4 号 | 在最终 MySQL 执行安全升级；回归 `/api/admin/students`、批次生成、存证、撤销、补发和统一错误响应；检查空库 DEMO 回退；核对写接口 RBAC；运行测试 | 数据库升级结果；接口回归结果；权限矩阵；测试日志 | 新旧证书、哈希、回执、状态和审计日志可追踪；未授权写操作被拒绝；后端测试通过 |
| 5 号 | 按最终 `PUBLIC_VERIFY_BASE_URL` 重生成固定证书、二维码和回执；核对 PDF 哈希、回执链和补发产物；测试链只做可选验证 | 固定证书包、二维码包、哈希/回执清单 | 编号唯一；二维码地址正确；哈希与 PDF 一致；链不可用时本地链仍可演示 |

### 7 月 17 日：演示证据与课程交付日

| 负责人 | 明日任务 | 明日必须交付 | 验收标准 |
| --- | --- | --- | --- |
| 1 号 | 组织完整答辩脚本彩排；整合需求、概要、详细设计、测试计划、部署说明和 PPT；确认 `dev` 为最终提交基线；验收通过后再开 `dev→main` PR | 完整演示录屏/截图索引；课程文档目录；最终阻塞清单；稳定版 PR | 从导入到撤销后二次验真一遍跑通；所有材料引用真实实现；PR 前无敏感文件和未验证代码 |
| 2 号 | 补齐管理员端最终截图；核对 ADMIN/TEACHER/AUDITOR 权限、批量删除、撤销补发反馈；只修复影响演示的 UI/契约问题 | 最终管理员端截图包；角色权限表；构建结果 | 三类角色入口和权限正确；不再出现笼统 Network Error |
| 3 号 | 在已有 `CERT-20260716-0001` 实扫通过基础上，扩展旧二维码、撤销/补发、篡改、未知编号和回执缺失结果；整理公共验真演示话术 | 手机扫码证据；公共验真截图/录屏；异常测试表 | 各类二维码进入前端公共验真页；旧证书 `REISSUED`、撤销状态展示正确 |
| 4 号 | 输出最终数据库结构/迁移说明、健康检查和接口运行手册；复查异常 HTTP 分支、审计记录、空库 DEMO 回退和后端 RBAC；不在最后一天扩展正式 JWT | 后端部署说明；接口清单；最终测试报告；权限回归结果 | 他人按说明能启动后端并完成核心接口调用；未授权写接口不会被假阳性放行 |
| 5 号 | 整理已实扫通过的 `CERT-20260716-0001` 及其他证书 PDF、二维码、哈希、回执、篡改样本和本地链回退包；若 Ganache 稳定则留一张回执证据，否则明确使用本地链 | `outputs` 演示证据索引；本地链回退说明；安全检查结果 | 不含 `.env`、私钥、真实学生数据；答辩现场可不依赖链节点 |

### 两日统一禁止事项

- 不新增正式学生登录、DID/VC、完整可信简历或复杂链上功能。
- 不把 `student_no` 演示参数表述成正式身份认证。
- 不在答辩前临时重构主链路或切换未验证分支。
- 不提交 `.env`、私钥、Token、真实学生资料或构建缓存。
- GitHub PR 已全部合并但没有可依赖的 review/checks 证据；最终稳定版必须以本地测试、真实环境回归和 1 号验收为合并门槛。

## 2026-07-16 验收结果

### 已通过

- 后端自动化测试：`77 passed，5 skipped`；5 个跳过项均为缺少 `eth_tester` 的可选测试链测试。
- 后端 `compileall` 通过。
- 前端 TypeScript 检查和 Vite 生产构建通过：`1715 modules transformed`。
- FastAPI `/api/health`、FastAPI `/docs` 和 Vite 首页本地 HTTP 冒烟均返回 `200`。
- QA harness 的 Python 语法检查通过，报告位于 `outputs/qa-20260716/`。

### 尚未通过或未执行

- 上述 pytest 使用内存 SQLite fixture，不能替代最终 MySQL 建表、迁移、重启持久化和真实接口回归。
- 自动化浏览器完整交互尚未完成；Playwright CLI 因本机浏览器服务不可用而失败，但已有一条人工手机浏览器 PASS 证据。
- 尚未完成固定数据的导入 → 批量生成 → 存证 → 学生下载 → 扫码/编号验真 → 篡改复验 → 撤销 → 补发完整链路。
- 已完成一张新证书二维码手机实扫；仍未完成旧二维码、撤销/补发二维码、最终局域网地址、后端批次/模板/证书写接口 RBAC 及 `ADMIN/TEACHER/AUDITOR` 的真实 401/403 分支验收。

### 验收结论

当前等级为：**代码与构建通过、进程级冒烟通过、最终业务验收部分通过**。

不能写成“完整闭环验收通过”。最终放行条件是：真实 MySQL 持久化回归、浏览器业务流程、手机扫码、撤销/补发关联、异常验真和后端权限分支全部留存证据。

## 2026-07-17 联调记录

### 今日目标

- 以 `dev@274d499` 为验收基线，完成 1 号收口检查；原本的本地改动已提交到远端备份分支，再选择性迁移文档。
- 固定当前 WLAN 的 `PUBLIC_VERIFY_BASE_URL=http://10.160.211.35:5173/public/verify`；Vue 使用 `--host 0.0.0.0`，前端保持 `VITE_USE_MOCK=false`。
- 运行后端测试、编译检查、前端生产构建和差异格式检查。
- 建立端到端验收矩阵，明确真实 MySQL、FastAPI、Vue 和手机扫码的剩余阻塞。

### 今日集成结果

| 模块 | 负责人 | 状态 | 说明 |
| --- | --- | --- | --- |
| 总体设计、系统集成与部署 | 1 号 | 代码和规则已统一到 dev，真实环境阻塞 | 远端 `dev@274d499` 已包含 `main` 全部提交；PR #25/#26 已完成同步和规则固化；建立 [2026-07-17 验收矩阵](2026-07-17验收矩阵.md) |
| 管理端前端 | 2 号 | 构建通过，待真实接口回归 | 前端构建 `1716 modules transformed`；管理员端真实截图、角色和存证操作仍待服务启动 |
| 学生与验真端 | 3 号 | 自动化覆盖，待浏览器/手机回归 | 公共验真状态已有代码和测试；今日未启动 5173，未新增手机证据 |
| 后端业务接口 | 4 号 | 测试通过，真实 MySQL 阻塞 | 最新隔离验证为后端 `79 passed，5 skipped`，`compileall` 通过；当前未配置 `DATABASE_URL`，未执行 MySQL 持久化回归 |
| 证书生成与存证 | 5 号 | 地址已固定，待重生成 | 本地哈希链为主线兜底；按 `http://10.160.211.35:5173/public/verify` 重生成最终二维码和补发产物 |

### 已完成检查

- 后端自动化测试：最新隔离验证为 `79 passed，5 skipped`。
- 后端 `compileall`：通过。
- 前端 TypeScript 检查和 Vite 生产构建：通过，`1716 modules transformed`。
- `git diff --check`：通过。

### 今日阻塞

| 问题 | 影响 | 负责人 | 处理计划 |
| --- | --- | --- | --- |
| 项目目录没有 `.env`，当前进程未设置 `DATABASE_URL` | 无法把 SQLite 测试结果升级为真实 MySQL 验收 | 4 号、1 号 | 由 4 号提供不含密钥的本机配置方法；配置后执行建表、迁移、重启持久化回归 |
| FastAPI 8000 和 Vue 5173 当前未启动 | 无法完成真实接口、浏览器和手机扫码证据 | 1 号、2 号、3 号、4 号 | 先统一启动方式和 `PUBLIC_VERIFY_BASE_URL`，再执行 E1–E11 |
| 当前无 `mysql` 客户端 | 不能直接验证数据库账号、表结构和迁移结果 | 4 号 | 使用项目 Python 依赖或已确认的 MySQL 工具完成连接检查，不打印密码 |
| 前端主 chunk 约 1.10 MB | 非阻塞性能风险 | 2 号 | 记录即可，答辩前不做非必要拆包 |

### 今日结论

当前为“代码和协作规则已统一到 `dev@274d499`、构建通过、真实环境业务验收未完成”。在真实 MySQL、FastAPI、Vue 启动并补齐撤销/补发、异常验真、权限和手机扫码证据前，不能写成完整闭环已验收。

### PR #24 收口处理

- PR #24 已由 3 号提交并误合并到 `main@3609e50`，补齐学生端和公共验真状态覆盖及验收清单。
- PR #25 已将其有效增量同步到 `dev@efe020d`，解决冲突并通过后端 `79 passed，5 skipped`、前端构建和差异检查。
- 当前 `origin/main` 是 `origin/dev` 的祖先，确认 `dev` 不缺少 `main` 内容；后续所有功能和文档 PR 统一以 `dev` 为 base。
- PR #26 已合并到 `dev@274d499`，GitHub 默认分支也已改为 `dev`，新建 PR 将默认指向集成分支。
