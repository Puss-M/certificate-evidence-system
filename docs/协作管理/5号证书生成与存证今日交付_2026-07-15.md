# 5号证书生成与存证今日交付 - 2026-07-15

## 今日完成

### 一、按计划完成的 P0 任务（今日分工.md 第4.5节）

- 用模拟学生批量生成 8 张证书样例（`CERT-20260715-0001` ~ `0008`），确认每张 `certificate_no` 唯一。
- 检查每张证书的二维码指向对应 `verify_url`，能在验真端查到记录。（手机扫码还没完成，我明天全部重新跑一次来确认）
- 对最终 PDF 计算 SHA-256，确认数据库 `certificate_hash` 与文件一致（交叉验证过）。
- 检查本地哈希链回执连续性：`receipt_id`、`previous_hash`、`current_block_hash`、`block_height`、`evidence_time` 均正常。
- 完成篡改 PDF 测试：修改文件后重新校验，得到哈希不一致结果。

### 二、完成任务（P2，团队确认后追加，非今天原定范围）

- **Merkle Root 批次存证层**：批次内证书哈希建 Merkle 树、算 Root，Root 链式关联（`previous_root_hash`/`current_root_hash`），Merkle Proof 生成与校验接口，撤销不影响 Root（已合并进 `main`、`dev`）。
- **本地测试链集成**：
  - 编写 `CredentialRootRegistry.sol` 合约（按批次存 Root，不逐张证书上链，跟 1 号自己那份按证书设计的合约区分开）。
  - 实现 `chain_service.py` 后端上链服务，任何配置缺失/连不上/写入失败都返回 `None`，不影响本地 Root 结果（降级策略）。
  - 用后端专属钱包（环境变量配置私钥），不依赖任何人的个人 MetaMask，解决了"网页端自动化不能绑定某个人设备"的问题。
  - 本机完整跑通：起 Ganache → 部署合约 → 建批次 → 生成证书 → 计算 Root → 拿到真实链上 `tx_hash`，并反向从链上读回数据核对一致。
- 后端测试：`62 passed`（含新增的 7 个链相关测试）。

## 涉及文件

- `backend/app/services/certificate_service.py`
- `backend/app/services/merkle_service.py`
- `backend/app/services/chain_service.py`
- `backend/app/models/credential_root.py`、`merkle_tree_node.py`
- `backend/app/schemas/batch.py`、`verification.py`
- `backend/app/api/routes/certificate_batches.py`、`verification.py`
- `backend/tests/test_merkle_service.py`、`test_chain_service.py`
- `chain/`（合约、编译产物、部署脚本、`README.md`）
- `outputs/certificates/CERT-20260715-0001` ~ `0008`（本地样例，不提交）

## 运行方式

```bash
cd backend && python -m pytest -q
```

本地测试链体验步骤见 `chain/README.md`（起 Ganache、部署合约、配置 `.env`）。

## 证据/结果

- 证书样例：`outputs/certificates/CERT-20260715-0001` ~ `0008`
- Merkle Root / Merkle Proof：测试全部通过，撤销后 Root 不受影响
- 本地链上链：`POST /api/admin/batches/{batch_id}/merkle-root` 返回真实 `tx_hash`（本机验证，链上读回数据与数据库一致）
- 相关截图另存于 `docs/协作管理/2026-07-15截图/`

## 需要联调

- 1号：review 本地测试链集成这个 PR（`feature/certificate-evidence` 分支）
- 3号：验真页面前端目前还没有提交，验真结果展示需要对接（非本模块能推进）

## 风险/阻塞

- 本地测试链（Ganache）是内存运行，进程重启或电脑重启后链上数据会丢失，需要重新部署合约；不影响本地 MySQL 里的 Root 主线记录。
- `chain/node_modules` 在部分环境下有残留安装问题，已通过 `.gitignore` 规避，不影响他人使用。
- 上链是 P2 加分项，任何一步没配置好都会自动降级为纯本地 Root 流程，不阻塞主线演示。

## 安全说明

- 不提交 `.env`、真实私钥、真实学生数据。
- `chain/deployment-info.json`（本机真实部署的合约地址）已加入 `.gitignore`，不提交。
- 证书样例全部使用模拟学生和模拟机构信息。
