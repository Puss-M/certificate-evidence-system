# 2026-07-17 今日开发任务分工

## 一、今日目标

今天不再扩展新的主线功能，重点完成：

1. 在同一版本、同一数据库、同一批模拟数据下跑通完整证书闭环。
2. 补齐管理员端、学生端、公共验真端、手机扫码和异常状态的验收证据。
3. 完成课程设计文档、测试计划、答辩材料和稳定版合并准备。

## 二、当前基线与风险

- 验收基线：本地与远端 `dev` 已同步到 `274d499`；当前文档分支 `codex/docs-20260717-delivery` 从该提交创建。
- 当前 WLAN 的公共验真地址已固定为 `PUBLIC_VERIFY_BASE_URL=http://10.160.211.35:5173/public/verify`；Vue 必须使用 `--host 0.0.0.0` 启动，保持 `VITE_USE_MOCK=false`，手机与电脑连接同一 WLAN，不使用 VPN 地址 `198.18.0.1`。
- GitHub PR #1–#26 已合并；PR #25 已同步代码，PR #26 已固化“所有 PR 先进入 dev”的规则，暂无可依赖的 CI 检查。
- 最近关键 PR：
  - PR #23：管理员端批次学生选择、Merkle Root 展示、公共验真 Merkle Proof 联动。
  - PR #22/#21/#18：管理员端批量删除、角色跳转、交付文档和截图。
  - PR #19/#17：后端删除、补发、批次生成异常修复和 `college` 字段持久化。
  - PR #20/#15：批次 Root、本地测试链适配和降级策略。
- PR #16：学生端和公共验真端。
- PR #24：3 号补齐学生端/公共验真状态覆盖、验收清单和 `REISSUED`/`NO_RECEIPT` 测试，曾误合并到 `main@3609e50`。
- PR #25：将 PR #24 的有效增量合并到 `dev@efe020d`，解决两个前端冲突、清理重复类型和 `desktop.ini`，并修复 Mock 补发证书误判为 `PASS`。
- PR #26：把“所有功能、修复和文档 PR 统一指向 `dev`，仅验收后的 `dev → main` 可进入稳定分支”写入团队协作规则。
- 项目文档评估：P0 代码约 95%，联调验收约 65%–70%，课程材料约 50%。
- 原本的 8 个本地未提交文件已归档到远端备份分支 `codex/local-work-backup-20260717@bbf9359`；仅选择性迁移已审计的文档，不整包合并备份分支。
- 旧基线 `a45a038`、旧测试数和“PR #1–#22”等当前状态表述由 1 号统一校准；历史日期记录保留当时的真实结果。

## 三、五人任务分工

### 1 号：总体设计、系统集成与部署

**任务**

- 冻结版本、端口、数据库、固定模拟数据和 `PUBLIC_VERIFY_BASE_URL`。
- 审计已归档的本地改动，仅将确认过的文档迁移到最新 `dev` 基线。
- 建立端到端验收矩阵，组织真实 MySQL + FastAPI + Vue 全流程。
- 整合需求规格、概要设计、详细设计、测试计划、部署说明、答辩 PPT 和演示脚本。
- 验收全部通过后，再准备 `dev → main` 稳定版 PR。

**必须交付**

- 端到端验收矩阵。
- 固定演示数据和证据索引。
- 最终阻塞清单。
- 更新后的项目基线和课程材料目录。

**验收门槛**

完整跑通：模板配置 → Excel 导入 → 创建批次 → 批量生成 PDF/二维码/哈希 → 存证 → 学生下载 → 手机扫码 PASS → 篡改 PDF 失败 → 撤销后二次验真 → 补发及新旧证书追踪。

### 2 号：管理员端前端与存证展示

**任务**

- 使用真实接口模式 `VITE_USE_MOCK=false` 回归管理员端。
- 验证 PR #23 的学生选择、批次 Merkle Root、链哈希和交易哈希展示。
- 回归导入、批次、证书、存证回执、操作日志、批量删除、撤销、补发和角色路由。
- 核对 ADMIN、TEACHER、AUDITOR 的菜单和操作反馈。

**必须交付**

- 管理员端最终截图包。
- 三类角色权限表。
- 前端生产构建日志。
- 前端问题清单。

**验收门槛**

- 刷新页面后真实数据仍存在。
- 三类角色入口和权限正确。
- 不再出现笼统的 `Network Error`。
- `npm run build` 通过。

### 3 号：学生端、公共验真与手机扫码

**任务**

- 回归学生列表、详情、PDF 下载、二维码和公共验真页面。
- 覆盖 `PASS`、`HASH_MISMATCH`、`REVOKED`、`REISSUED`、`NOT_FOUND`、`NO_RECEIPT`。
- 实扫有效证书、撤销证书、补发后的旧二维码和新二维码。
- 整理公共验真页面的演示话术。

**必须交付**

- 学生端和公共验真截图/录屏。
- 异常测试表。
- 原始 PDF、篡改 PDF 和验真结果。
- 手机扫码证据。

**验收门槛**

- 所有二维码进入最终 Vue 公共验真页。
- 原始文件通过，篡改文件失败。
- 撤销证书不再显示有效。
- 旧证书显示 `REISSUED`，新证书可正常验真。

### 4 号：FastAPI、MySQL、权限与接口

**任务**

- 执行最终 MySQL 建表、迁移、重启持久化和健康检查。
- 回归学生、批次生成、存证、撤销、补发、审计日志和统一错误响应。
- 验证后端权限：未登录返回 `401`，错误角色写操作返回 `403`。
- 验证重复撤销、重复补发和错误状态操作返回明确的 `409`。
- 检查空库 DEMO 回退风险，补充部署和启动说明。
- 不在最后一天扩展正式 JWT。

**必须交付**

- 数据库迁移结果。
- 接口运行手册和健康检查结果。
- 权限矩阵。
- 后端最终测试日志。

**验收门槛**

- 重启后数据仍在。
- `certificate_no`、PDF、哈希、回执、状态和审计日志可互相追踪。
- 未授权写操作不会被前端隐藏按钮误导，后端实际返回正确状态码。
- 后端测试通过。

### 5 号：证书生成与可信存证

**任务**

- 按最终 `PUBLIC_VERIFY_BASE_URL` 重生成固定证书、二维码、哈希和回执。
- 分别准备撤销证书和补发证书，核对新旧产物关联。
- 核对 PDF SHA-256、本地哈希链连续性和篡改样本。
- 测试链只做可选验证；连接失败时保持本地哈希链演示。

**必须交付**

- `outputs` 演示证据索引。
- 固定证书、PDF、二维码和篡改样本包。
- 哈希与回执清单。
- 本地链降级说明。
- 安全检查结果。

**验收门槛**

- 证书编号唯一，二维码地址正确。
- PDF 与存证哈希一致。
- 补发生成新编号、PDF、二维码、哈希和回执。
- 不含 `.env`、私钥、Token 或真实学生数据。

## 四、协作顺序

1. **环境门禁：1 号、4 号、5 号**
   - 固定 `dev@274d499`；本地 `dev` 已完成归档和安全快进，后续提交统一通过文档分支 PR 进入 `dev`。
   - 启动 MySQL、FastAPI、Vue。
   - 确认 `VITE_USE_MOCK=false`、Vue 使用 `--host 0.0.0.0`，二维码统一指向 `http://10.160.211.35:5173/public/verify`。

2. **模块回归：2 号、3 号、4 号、5 号并行**
   - 4 号先确保后端和数据库真实可用。
   - 5 号基于最终地址生成二维码。
   - 2 号回归管理员端，3 号回归学生/公共验真和手机扫码。

3. **端到端彩排：1 号组织全员**
   - 必须使用同一数据库、同一批数据、同一批二维码。
   - 证据至少包含数据库记录、接口响应、浏览器截图、手机扫码、审计日志和哈希清单。

4. **材料门禁：1 号整合**
   - 各模块先提交事实、截图、日志和测试结果。
   - 1 号核对文档只描述真实实现，再决定是否发起 `dev → main` PR。

## 五、今日禁止事项

- 不新增正式学生登录、DID/VC、完整诚信简历或复杂链上功能。
- 不在答辩前大规模重构主链路或做非必要性能优化。
- 不把 `student_no` 演示参数表述成正式身份认证。
- 不把“PR 已合并”表述成“系统已完整验收”。
- 不提交 `.env`、私钥、Token、真实学生资料或构建缓存。
- 不整包合并备份分支，不重新带入已判定为旧版本的前端文件或未经审计的本地改动。

## 六、最终放行标准

只有同时满足以下条件，才允许把 `dev` 合并到 `main`：

- 真实 MySQL 持久化回归通过。
- 后端测试、前端构建和权限测试通过。
- 导入、签发、存证、下载、验真、篡改、撤销、补发全流程跑通。
- 有效、撤销、补发旧/新二维码完成手机扫码证据。
- 课程文档、测试计划、部署说明和答辩材料引用真实实现。
- 仓库无密钥、Token、真实个人信息和未验证代码。
