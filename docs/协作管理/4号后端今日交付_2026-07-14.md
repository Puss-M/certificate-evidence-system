# 4号后端今日交付 - 2026-07-14

## 今日完成

- 搭建 FastAPI 后端项目骨架。
- 完成 MySQL 连接配置，提供 `.env.example`，真实 `.env` 不提交。
- 建立基础目录：`app/api`、`app/models`、`app/schemas`、`app/services`、`app/db`、`app/core`。
- 实现统一返回格式 `ApiResponse`。
- 实现 CORS 配置，允许本机 Vue 3 开发服务联调。
- 实现全局异常处理初版，避免向前端返回 Python 错误栈。
- 建立核心表结构草案：学生、证书模板、证书批次、证书、存证回执、撤销记录、审计日志。
- 实现最小接口：健康检查、数据库检查、学生列表、证书列表、公共验真接口。
- 对接 5 号证书生成与本地哈希链回执，补齐管理员前端接口。
- 补充接口自动化测试，当前测试结果为 `15 passed`。

## 涉及文件

- `backend/README.md`
- `backend/.env.example`
- `backend/requirements.txt`
- `backend/requirements-dev.txt`
- `backend/app/main.py`
- `backend/app/api/router.py`
- `backend/app/api/routes/health.py`
- `backend/app/api/routes/students.py`
- `backend/app/api/routes/certificates.py`
- `backend/app/api/routes/verification.py`
- `backend/app/core/config.py`
- `backend/app/core/responses.py`
- `backend/app/core/exceptions.py`
- `backend/app/db/base.py`
- `backend/app/db/session.py`
- `backend/app/models/*.py`
- `backend/app/schemas/*.py`
- `backend/scripts/create_tables.py`
- `backend/scripts/upgrade_certificate_schema.py`
- `backend/tests/test_health.py`
- `backend/tests/test_mock_endpoints.py`
- `backend/tests/test_admin_frontend_contract.py`
- `.gitignore`

## 运行方式

在 `backend` 目录下执行。

先启动 MySQL。下面是命令格式，按本机实际路径替换，不要把真实路径和密码提交到仓库：

```powershell
"<MYSQL_HOME>\bin\mysqld.exe" --defaults-file="<MYSQL_DATA_DIR>\my.ini" --console
```

再启动 FastAPI：

```powershell
.\.venv\Scripts\python.exe -m uvicorn app.main:app --reload
```

打开 Swagger：

```text
http://127.0.0.1:8000/docs
```

运行测试：

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

## 接口结果

- `GET /api/health`：后端健康检查。
- `GET /api/health/db`：MySQL 连接检查。
- `GET /api/students`：学生列表 mock 数据。
- `GET /api/certificates`：证书列表 mock 数据。
- `GET /api/verification/{certificate_no}`：公共编号验真接口。
- `POST /api/verification/{certificate_no}/file`：上传 PDF 哈希复验接口。
- `GET /api/admin/certificates`：管理员证书列表。
- `GET /api/admin/evidence/receipts`：管理员存证回执列表。
- `GET /api/admin/evidence/integrity`：本地哈希链完整性检查。

公共验真状态：

- `PASS`
- `REVOKED`
- `REISSUED`
- `HASH_MISMATCH`
- `NOT_FOUND`
- `NO_RECEIPT`
- `SYSTEM_ERROR`

## 需要联调

- 2号管理端前端可对接 `/api/admin/...` 管理员接口。
- 3号公共验真端可对接 `GET /api/verification/{certificate_no}` 和上传 PDF 复验接口。
- 5号证书生成模块已经把真实 `certificate_hash`、`pdf_path`、`qr_code_path`、`receipt_id` 写入数据库。

## 风险/阻塞

- 管理员前端部分辅助接口仍使用演示数据，证书、回执和验真主链路已接数据库。
- 当前存证为本地哈希链，FISCO BCOS 测试链接口后续继续扩展。
- 当前登录为本地演示账号，正式用户表、JWT 和角色权限后续完善。
- 后续写入审计日志时，不能记录密码、密钥、真实身份证号、手机号或完整敏感请求体。

## 安全说明

- 不提交 `.env`。
- 不提交数据库真实密码、JWT 密钥、API key、私钥或助记词。
- 不使用真实学生数据。
- 当前 mock 数据仅使用测试学生和测试证书编号。
